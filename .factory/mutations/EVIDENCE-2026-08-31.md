# Mutation kill-rate 证据 — 2026-08-31（wop-web-tools）

- 命令：`python3 .factory/mutations/run.py`（全量，无 --only 过滤）
- 退出码：**0**（全部按预期；无 FAIL / SKIP / 配置错误；还原零残留——runner 内置逐文件字节校验通过）
- 缺陷集：13 条 = 篡改类 6（guard 门）+ 行为破坏类 5（tests 门）+ 负例 2（guard/tests 各 1）

## 汇总（原文摘录）

```
===== mutation 冒烟汇总 =====
  [G-01] PASS       blocked=True（rc=1）符合预期
  [G-02] PASS       blocked=True（rc=1）符合预期
  [G-03] PASS       blocked=True（rc=2）符合预期
  [G-04] PASS       blocked=True（rc=1）符合预期
  [M-01] PASS       blocked=True（rc=2）符合预期
  [M-02] PASS       blocked=True（rc=2）符合预期
  [B-01] PASS       blocked=False（rc=0）符合预期
  [B-101] PASS      blocked=True（rc=1）符合预期
  [B-102] PASS      blocked=True（rc=1）符合预期
  [B-103] PASS      blocked=True（rc=1）符合预期
  [B-104] PASS      blocked=True（rc=1）符合预期
  [B-105] PASS      blocked=True（rc=1）符合预期
  [B-106] PASS      blocked=False（rc=0）符合预期
  篡改类拦截（guard 门）: 6/6 = 100%
  行为破坏类拦截（tests 门）: 5/5 = 100%
  正向缺陷拦截（kill rate）: 11/11 = 100%
  负例放行: 2/2
  结论: 门灵敏度冒烟通过（auto-merge 的必要非充分条件）
  周界指纹已绑定: c06663275c19（evidence-stamp.json）
```

## 行为破坏类逐条击杀证据（注入 → 变红的检查项，注入后实测）

| id | target | 注入 | kill 证据（注入后实测变红） |
|---|---|---|---|
| B-101 | assets/wf14.js | t() 未注册 key 丢弃 fallback 回显 key（`return String(fallback);` → `return k;`） | wf14 自测 SELFTEST 13/15：断言 3（WF14-T-FALLBACK 回退 fallback/回显 key）+ 断言 4（WF14-NEG 空 fallback）红 |
| B-102 | assets/wf14.js | applyDom 未注册 key 不再跳过（`if (v === undefined) continue;` → 恒 false 分支） | wf14 自测断言 6（WF14-DOM-SKIP 他人片段文案不被清空）红 |
| B-103 | gm/gmcore.mjs | 黄金向量公钥末字节改坏（pubB64 …YKg → …YKh） | gm/test.mjs `FAIL GM-12 密钥 Base64 分发格式 << 公钥 b64 不一致`，gmcore 23/24（含 GM-12b 私钥推导与黄金公钥失配），rc=1 |
| B-104 | gm/gmcore.mjs | 黄金 SM3 摘要末位 hex 改坏（…7cf → …7cd） | gm/test.mjs 黄金 sm3 比对红（GM-01 SM3 黄金摘要族），rc=1 |
| B-105 | index.html | tab 页改名（id="tab-request" → "tab-request-X"） | dom_check `FAIL DOM-4 六 tab 页存在且互不嵌套 [tab-request 缺失]`，rc=1 |

每条 find 均经预验证「恰好命中一次 + 注入后对应检查真红 + 字节还原一致」后才入缺陷集。

## 击杀真实性抽验（手动复放，≥3 样本）

按「数字本身不是证据」口径，全量跑之外手动复放 4 个样本独立复核：

| 样本 | 复放命令（注入态） | 实测 |
|---|---|---|
| B-101 | `cat assets/wf14.js assets/wf14.selftest.js <CI 同源 runner> \| node` | SELFTEST 13/15，rc=1（真击杀） |
| B-103 | `node gm/test.mjs` | `FAIL GM-12 公钥 b64 不一致`，gmcore 23/24，rc=1（真击杀） |
| B-105 | `node dom_check.mjs` | `FAILED: DOM-4 [tab-request 缺失]`，rc=1（真击杀） |
| M-01 | `python3 .factory/guard.py --files assets/wf9.js` | rc=2：PERIMETER 与 MISSION 周界清单不一致（MISSION 独有 docs/spec.md），fail-closed 拦截（真拦截） |

复放后 `git status -s` 零残留（注入全部字节还原）。

## 口径说明

- tests 门命令：`scripts/run_tests.sh --no-lock`（L3 判据锚定 run_tests.sh）= CI/pre-commit
  同源五项：① `node --check assets/*.js` 全量语法门 ② wf14 自测 15 项（CI 同源拼接形态）
  ③ `dom_check.mjs` DOM-1..5 结构矩阵 ④ `scan_banned.mjs` SCAN-1..4 禁词
  ⑤ `gm/test.mjs` 黄金向量 24 项 + 产物漂移；退出码收敛 0/1（127 缺 node 亦 = 1，fail-closed）。
- 退出码语义：rc=1 = 击杀证据；rc=0 = 放行；域外退出码/超时 = 无效运行（judge 判 FAIL，
  既不奖励击杀也不奖励放行）。guard 门 rc=2 为 fail-closed 设计（=拦截）。
- 门超时预算：guard 300s / tests 600s（tests 门实测基线 ~0.7s，850 倍余量）。
- 行为类 kill rate 100%（≥80% 为下限；低于 100% 即缺陷选材或门有洞）。
- 本证据为「必要非充分」条件；auto-merge 的开启属治理动作，由人类决定（本仓默认关闭）。
- 缺陷集为 wop-web-tools 本地化（mutations/ 按 DISTRIBUTION.json 属 skip 分发，每仓一份）；
  源自 wop-php-sdk S1 缺陷集形态（移植模板 6401fb0），B-1xx 目标全部改指本仓资产
  （assets 切片 / gm 国密内核 / 壳 DOM 结构）；本仓无库 API 面，docstring 门不启用（无 D 类）。
- 周界指纹 `c06663275c19…` 绑定本仓 factory-local.json 的 git blob（evidence-stamp.json）；
  stamp 的 evidence 字段已修正指向本文件（runner 默认占位为上游首证日期，非指纹域）。
