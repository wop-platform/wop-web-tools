# WOP Web Tools — Spec

> 状态：草案（grill 后冻结） · 版本：v0.1-draft · 日期：2026-08-30
> 对齐：crypto-strategy-spec v0.3-reviewed · wop-sdk-spec v1.0-ratified · wop-skills SECURITY.md S1–S8

## 0. 参考真源

- [wop-specs/crypto-strategy-spec.md] 协议契约（D1–D13、I1–I7）——**协议术语唯一权威**
- [wop-specs/crypto-vectors.json] 黄金向量——**字节级正确性锚**
- [wop-specs/sdk/wop-sdk-spec.md] SDK 契约（F1–F9、A1–A7）
- [wop-skills/SECURITY.md] 安全纪律精神（S1–S8）
- 现有实现：`gtsp-wop-gateway/docs/tools/rsa-keygen.html`（迁移源，行为基线）

## 1. 范围

浏览器端纯静态工作台：密钥生成与自检、WOP 报文构造/验证/模拟、联调辅助与错误诊断。
不含后端服务、不含真实网关调用、不含私钥持久化。

## 2. 功能条款

### 2.1 密钥（F1–F3，继承现状为基线）

- **F1 密钥生成**：RSA 3072 / 4096；私钥 PKCS#8、公钥 X.509 SPKI；PEM 与单行 Base64 双格式；复制/下载。
- **F2 密钥自检**：生成后必做三项自检，任一失败即拒绝输出——
  SHA256withRSA 签名/验签往返、RSA-OAEP(SHA-256) 加解密往返、模数位数与所选一致。
- **F3 公钥指纹**：输出 SPKI 的 SHA-256 指纹（冒号分隔 hex）；联调页商户私钥填入后自动派生指纹并提示与平台比对。

### 2.2 报文构造（F4）

- **F4 构造请求**（商户 → 网关）：
  - 输入 appKey / 请求路径 / expiredSeconds / 加密级别 L0|L2 / 安全套件 / 业务 JSON body；
  - 产出 canonicalRequest（5 段 `\n`、Java URLEncoder 语义、ASCII 头排序）与
    `x-wop-sign`（`v1/<expired>/<signedHeaders>/<b64url 签名>`）、`x-wop-content-digest`（`<alg> <恰一空格> <64 位小写 hex>`）；
  - L2：AES-256-GCM 全文加密 → `{"encrypted":…}`；DEK 载荷 `AES-256-GCM$b64url(key)$b64url(iv)` 以 RSA-OAEP(SHA-256) 包装入 `x-wop-encrypt`；
  - 产出请求头表、线上请求体、可复制 curl 命令；
  - 本地预检：以商户公钥对 canonical 验签（等价网关 SignFilter 重建路径）。

### 2.3 报文验证（F5）

- **F5 验证平台报文**（同步响应 / 异步回调）：
  - 固定顺序：① 摘要复核 → ② 解析 x-wop-sign → ③ 平台公钥验签 → ④ DEK 解包 → ⑤ alg 族比对 → ⑥ bulk 解密；
  - canonical URI：同步响应取网关请求路径，异步回调取回调 URL 的 path；
  - 步骤级可视化（✓/✗ + 失败原因），支持"模拟平台响应/回调"闭环自测（联调密钥仅存会话内存）。

### 2.4 扩展能力（F6+，按优先级）

**P0（随 MVP 发布）**

- **F6 SM2 密钥生成**（D2 待决）：生成 SM2-SM3 套件密钥（私钥 PEM/DER、公钥未压缩点），
  补齐"仅支持 3072/4096"的过时断言；依赖须审计 + 版本锁定 + 内置（S3）。
- **F7 向量自测**：内置黄金向量（crypto-vectors.json 子集），页面一键跑字节级断言，
  通过才提示"本工具与官方规格对齐"；向量版本号随发布物声明。
- **F8 回调推演标注**：回调契约未冻结前，页面显著 banner 标注 beta/推演，不宣称正式（S6）。

**P1（路线图一期）**

- **F9 整体粘贴解析**：粘贴原始 HTTP 请求（含全部头 + 体）自动拆分填入验证表单，免手抄头。
- **F10 错误诊断**：粘贴错误响应 → 语义 + HTTP 映射 + 修复建议 + 排查路径（对齐 wop-troubleshoot 62 错误码目录）。
- **F11 代码片段生成**：同一请求产出六语言官方 SDK 调用片段（语义对齐 wop-sdk-spec，非手写易错版）。
- **F12 canonical 可视化**：canonicalRequest 逐段高亮展示 + 字段来源标注，定位签名偏差。

**P2（路线图二期）**

- **F13 API 目录**：渲染 OpenAPI 3.1 契约（contracts 层），API 表单模板化生成请求。
- **F14 教学图解**：数字信封流程（L0/L2）交互式演示。
- **F15 i18n**：中英双语。

## 3. 安全条款（S1–S7，宪法级）

- **S1 零外发**：密钥材料与明文报文**永不离开浏览器**。页面不得发起任何网络请求、
  不得加载第三方脚本（含 CDN/统计/字体）。构建产物须可审计（无外部资源引用）。
- **S2 不落盘**：私钥仅存内存/剪贴板/用户主动下载的文件；页面刷新即失；
  不得写入 localStorage/sessionStorage/IndexedDB。
- **S3 依赖白名单**：引入密码库须审计 + 版本锁定 + 内置打包（禁止运行时网络加载）；
  依赖清单与版本写入发布物声明。
- **S4 安全上下文**：Web Crypto 需 HTTPS 或本地文件环境；检测到不安全上下文须显式告警并禁用相关功能。
- **S5 文案纪律**：禁止任何诱导"上传私钥/提交平台"的文案；生产密钥管理明确引导至 wop-cli（S1–S8 通道）。
- **S6 契约诚信**：推演/未冻结协议内容须显著标注 beta 并链接真源；禁止宣称"正式契约"。
- **S7 不变量对齐**：先验签后解密、digest 必入签名头、DEK alg 族比对先于 bulk 解密、
  密钥参与判定的失败一律模糊（对齐 I1/I4/I5/I7），页面行为不得违反。

## 4. 治理与发布条款（G1–G5）

- **G1 归属**：wop-platform 组织（D3 待决），仓库与 specs/skills 同层治理。
- **G2 Pages**：GitHub Pages 启用（HTTPS）；根路径部署 `index.html`；同时保留仓库内单文件可下载路径。
- **G3 对齐**：协议术语/头名/错误码以 wop-specs 为唯一权威；真源变更须同步修订页面并 bump 版本。
- **G4 版本**：向量变更 = 破坏性变更；发布物声明对齐三元组版本（crypto spec / sdk spec / vectors）。
- **G5 测试载体**：功能条款 → 浏览器端 self-test 页（向量断言）；`spec:<ID>` 标签可 grep 索引；
  否定式条款（S1/S2/S6）须有对应断言。

## 5. 验收标准（A1–A6）

- **A1** Pages 线上可访问（HTTPS），密钥生成全流程可用。
- **A2** 下载单文件 `index.html` 本地打开（file://），全功能可用（离线卖点成立）。
- **A3** F7 向量自测通过；页面声明的对齐版本与 wop-specs 一致。
- **A4** S6 标注在回调场景可见（beta/推演 + 真源链接）。
- **A5** 与 wop-cli 重叠功能行为一致：同一输入（appKey/path/body/密钥）下，
  canonicalRequest 与 x-wop-sign draft 与 `wop sign` 输出逐字节一致。
- **A6** 页面无任何网络请求（DevTools Network 面板为空），无第三方脚本。

## 6. 决策记录（D1–D7，TBD 待拍板）

| # | 问题 | 选项 | 建议 | 影响 |
|---|---|---|---|---|
| D1 | 回调契约未冻结，公开策略 | A 显著 beta 标注；B 等冻结再上线；C 首版仅密钥生成 | **A** | 立即发布，风险可控 |
| D2 | SM2 支持时机与依赖 | A P0 引入审计库；B P1 纯实现；C 首版仅 RSA | **A（若审计窗口允许，否则 B）** | 决定首版范围 |
| D3 | 仓库归属 | A wop-platform 组织；B 个人 im47cn | **A** | 生态统一 |
| D4 | 语言 | 中文优先 / 双语 | 中文优先 | 首版文案 |
| D5 | 产物形态 | 源码模块化 + 构建单文件 / 保持单文件 | 模块化 + 构建 | 维护性 |
| D6 | 数据来源 | 内置打包 / 可选在线拉取 | 内置打包（零网络） | S1 边界 |
| D7 | 私钥交付 | 复制/下载即唯一副本 + 引导备份 | 采纳 | 安全体验 |

## 7. 边界服务能力全景（头脑风暴产出）

```text
场景 A：API 调用（商户 → 网关）          场景 B：回调通知（网关 → 商户）
  F1–F3 密钥生命周期                        F5  回调验证台（验签/摘要/解密）
  F4  请求构造工作台                        F8  回调 beta 标注
  F11 六语言代码片段生成                    F9  原始 HTTP 整体粘贴解析
  F12 canonical 可视化调试                  F10 回调 curl 自测引导
  F10 错误诊断（62 错误码）                 F15 教学图解
  F13 API 目录（OpenAPI 3.1）

场景 C：信任与治理（横切）
  F7  黄金向量自测（对齐三元组）
  S1–S7 零外发 / 不落盘 / 依赖白名单
  G1–G5 归属 / Pages / 对齐 / 版本 / 测试载体
```

优先级：P0 = 现有两场景上线 + F6/F7/F8（堵缺口、立信任）→ P1 = F9–F12（对拍、诊断、片段）→ P2 = F13–F15。
